---
- name: Ensure oci oss bucket exists
  command: oci os bucket get --name {{ artifacts_container }}

- name: Upload to oci oss bucket
  command: oci os object put -bn {{ artifacts_container }} --name {{ artifact }} --file {{ artifact_path }} --content-type auto --force
  async: 3600
  poll: 10
